# Lesson Voice Chat - Testing Guide

## Что было реализовано:

### 1. Voice Practice Mode (Голосова практика)
- Модальное окно 80% x 80%
- Визуализатор (3 полосы)
- Кнопка микрофона (hold to speak)
- История чата
- AI ведет диалог по промптам урока
- Кнопка "Завершити та оцінити"

### 2. Role-Play Mode (Ролевая игра)
- То же модальное окно
- AI играет роль из сценария
- Голосовой ввод + текстовый ввод
- Диалог в рамках урока
- Оценка в конце

---

## Quick Start - Запуск и тестирование

### Шаг 1: Применить миграции

```bash
cd /Users/olegbonislavskyi/Sites/AI\ English
python3 manage.py migrate
```

### Шаг 2: Запустить сервер (если еще не запущен)

```bash
python3 manage.py runserver
```

### Шаг 3: Открыть урок

Откройте в браузере:
```
http://127.0.0.1:8000/chat/lesson/372/
```

### Шаг 4: Тестирование Voice Practice

1. Кликните на вкладку "🎤 Voice Practice"
2. Кликните кнопку "Start Voice Practice"
3. **Ожидаемое поведение:**
   - Появится модальное окно 80% x 80%
   - AI автоматически начнет диалог с приветствия
   - Будет проиграно аудио приветствия
   - В чате появится первое сообщение AI

4. **Проверить микрофон:**
   - Зажмите кнопку микрофона (круглая)
   - Говорите 3-5 секунд
   - Отпустите кнопку
   - AI должен ответить через 2-5 секунд

5. **Проверить текстовый ввод:**
   - Введите текст внизу
   - Нажмите Send (стрелка)
   - AI должен ответить

6. **Завершить:**
   - После 5-7 обменов нажмите "Завершити та оцінити"
   - Должны появиться результаты оценки

### Шаг 5: Тестирование Role-Play

1. Кликните на вкладку "🎭 Role-Play"
2. Кликните кнопку "Start Role-Play"
3. **Ожидаемое поведение:**
   - Модальное окно откроется
   - AI начнет в роли (например, barista)
   - Можно отвечать текстом или голосом

4. **Проверить:**
   - AI остается в роли
   - Диалог в рамках сценария
   - Оценка работает

---

## Troubleshooting - Решение проблем

### Проблема 1: Модальное окно не открывается

**Проверьте консоль браузера (F12):**

```javascript
// Должно быть:
window.lessonVoiceModal // object
```

**Если undefined:**
- Убедитесь что все скрипты загружены:
  - `/static/js/recorder.js`
  - `/static/js/bars-visualizer.js`
  - `/static/js/lesson-voice-modal.js`
  - `/static/js/lesson.js`

**Решение:**
```bash
python3 manage.py collectstatic --no-input
```

### Проблема 2: Кнопки не реагируют

**Проверьте в консоли:**

```javascript
// Кликните F12 → Console
lessonVoiceModal  // должно показать объект
```

**Если ошибка "lessonVoiceModal is not defined":**
- Перезагрузите страницу с очисткой кеша: Cmd+Shift+R (Mac) или Ctrl+Shift+R (Windows)

### Проблема 3: Ошибка 404 при запросе

**Проверьте URLs:**

```bash
# В консоли Django должны быть эти URL:
# /chat/lesson/<id>/voice-practice-chat/
# /chat/lesson/<id>/roleplay/start/
```

**Если их нет - перезапустите сервер:**

```bash
python3 manage.py runserver
```

### Проблема 4: Microphone не работает

**Проверьте разрешения:**
- Браузер должен запросить доступ к микрофону
- В Chrome: Settings → Privacy → Site Settings → Microphone
- Разрешите доступ для localhost

### Проблема 5: Audio не проигрывается

**Проверьте:**
1. В консоли: `SpeechService` должен генерировать TTS
2. В ответе от API: `audio_url` должен быть не null
3. Файл должен существовать: `/media/audio/vp_*.mp3`

---

## Debug Console Commands

Откройте консоль браузера (F12) и выполните:

```javascript
// Проверить что modal существует
console.log(window.lessonVoiceModal);

// Проверить что можно открыть вручную
window.lessonVoiceModal.open(372, 'voice_practice');

// Проверить что CSRF token есть
console.log(document.querySelector('[name=csrfmiddlewaretoken]')?.value);

// Проверить что кнопки существуют
console.log(document.querySelector('.btn-start-voice'));
console.log(document.querySelector('.btn-start-roleplay'));
```

---

## Expected API Responses

### Voice Practice Start:
```json
{
  "session_id": 123,
  "initial_message": {
    "id": 456,
    "role": "model",
    "content": "Hello! Let's practice...",
    "translation": "Привіт! Давайте попрактикуємо...",
    "audio_url": "/media/audio/vp_123_init.mp3"
  },
  "continued": false
}
```

### Role-Play Start:
```json
{
  "session_id": 789,
  "ai_message": "Hi! Welcome to our coffee shop...",
  "initial_message": {
    "role": "model",
    "content": "Hi! Welcome to our coffee shop..."
  },
  "scenario_name": "Coffee Shop Encounter",
  "continued": false
}
```

### Evaluation Result:
```json
{
  "overall_score": 8.5,
  "pronunciation_score": 8.0,
  "grammar_score": 9.0,
  "vocabulary_score": 8.5,
  "fluency_score": 8.0,
  "task_completion_score": 9.0,
  "feedback": "Чудова робота! Ви добре використовуєте словник уроку...",
  "strengths": [
    "Good pronunciation",
    "Used lesson vocabulary correctly"
  ],
  "improvements": [
    "Work on fluency",
    "Practice more complex sentences"
  ]
}
```

---

## Next Steps After Testing

1. Если все работает - можно продолжать использовать
2. Если есть ошибки - пришлите скриншот консоли (F12)
3. Проверьте Django logs для backend ошибок:
   ```bash
   # В терминале где запущен runserver
   # Должны быть логи запросов
   ```

---

## Files Modified

### Backend:
- ✅ `apps/chat/models.py` - добавлены поля
- ✅ `apps/chat/views.py` - 5 новых views
- ✅ `apps/chat/urls.py` - 5 новых routes
- ✅ `apps/chat/services/gemini.py` - 2 новых метода
- ✅ `apps/chat/services/roleplay_engine.py` - lesson_context
- ✅ `apps/chat/migrations/0015_add_lesson_voice_fields.py` - миграция

### Frontend:
- ✅ `templates/learning/lesson_detail.html` - modal HTML
- ✅ `static/js/lesson-voice-modal.js` - новый файл
- ✅ `static/js/lesson.js` - обновлены init функции
- ✅ `static/css/learning.css` - modal стили

### Tests:
- ✅ `apps/chat/tests/test_lesson_voice.py` - unit + integration + QA checklist
